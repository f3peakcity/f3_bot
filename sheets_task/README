```
gcloud functions deploy f3-sheets-handler --entry-point f3_sheets_handler --region=us-east1  --allow-unauthenticated --trigger-http --runtime python39
```

Ensure that the database has been initialized:

```
cd sheets_task
pip install -r requirements.txt
export COCKROACH_CONNECTION_STRING="cockroachdb://user:pass@free-tier.gcp-us-central1.cockroachlabs.cloud:26257/defaultdb?sslmode=verify-full&sslrootcert=$HOME/.postgresql/root.crt&options=--cluster%3Dcluster_name"
python model.py
```